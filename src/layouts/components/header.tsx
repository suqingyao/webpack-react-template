import { Layout } from "antd";

const { Header } = Layout;

export const LayoutHeader = () => {
  return (
    <Header className="">
      <h1 className="text-white">Header</h1>
    </Header>
  );
};
